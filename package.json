{
  "name": "park-agent-launcher",
  "version": "1.0.1",
  "description": "Parallel Agent Runtime for Kiro",
  "main": "electron/main.js",
  "author": "13shivam",
  "license": "MIT",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../electron && npm install",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build": "run-p build:backend build:frontend",
    "build:sequential": "run-s build:backend build:frontend",
    "dev": "npm run build && cd electron && npm run dev",
    "package:mac": "npm run build && cd electron && npm run package:mac",
    "package:win": "npm run build && cd electron && npm run package:win",
    "clean": "rm -rf backend/dist frontend/dist electron/dist dist"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "npm-run-all": "^4.1.5"
  }
}
